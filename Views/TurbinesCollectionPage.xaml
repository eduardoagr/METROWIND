<ContentPage
    x:Class="METROWIND.Views.TurbinesCollectionPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:constant="clr-namespace:METROWIND.Constants"
    xmlns:controls="clr-namespace:METROWIND.Controls"
    xmlns:editors="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
    xmlns:inputLayout="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:local="clr-namespace:METROWIND"
    xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:model="clr-namespace:METROWIND.Models"
    xmlns:picker="clr-namespace:Syncfusion.Maui.Picker;assembly=Syncfusion.Maui.Picker"
    xmlns:rex="clr-namespace:METROWIND.Resources"
    xmlns:sfPopup="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
    xmlns:sys="clr-namespace:System;assembly=netstandard"
    xmlns:vm="clr-namespace:METROWIND.ViewModel"
    x:Name="TurbinesCollection"
    x:DataType="vm:TurbinesCollectionPageViewModel">

    <ContentPage.Behaviors>
        <mct:EventToCommandBehavior
            Command="{x:Binding PageEnterCommand}"
            CommandParameter="{x:Binding Source={x:Reference TurbineCollection}}"
            EventName="Appearing" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <rex:TurbineTemplate />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">
        <inputLayout:SfTextInputLayout
            Margin="10"
            ContainerType="Outlined"
            OutlineCornerRadius="8"
            WidthRequest="{OnIdiom Desktop=450,
                                   Phone=250}">
            <inputLayout:SfTextInputLayout.HintLabelStyle>
                <inputLayout:LabelStyle TextColor="{x:AppThemeBinding Dark={x:StaticResource White}, Light={x:StaticResource Black}}" />
            </inputLayout:SfTextInputLayout.HintLabelStyle>
            <editors:SfComboBox
                x:Name="combobox"
                DisplayMemberPath="Turbine.Name"
                ItemsSource="{x:Binding Turbines}"
                Style="{x:DynamicResource TurbinesCollectionComboBoxStyle}">
                <editors:SfComboBox.Behaviors>
                    <mct:EventToCommandBehavior
                        Command="{x:Binding SelectedItemChangeCommand}"
                        CommandParameter="{x:Binding Source={x:Reference combobox}}"
                        EventName="SelectionChanged" />
                </editors:SfComboBox.Behaviors>
            </editors:SfComboBox>
        </inputLayout:SfTextInputLayout>

        <CollectionView
            x:Name="TurbineCollection"
            Grid.Row="1"
            ItemTemplate="{x:StaticResource SelectDataTemplateSelector}"
            ItemsSource="{x:Binding Turbines}"
            Style="{x:StaticResource TurbineCollectionViewStyle}" />

        <Button
            Grid.Row="1"
            Command="{x:Binding AddNewTurbinePopUpCommand}"
            CommandParameter="{x:Binding Source={x:Reference TurbinePopUp}}"
            Style="{x:StaticResource OpenPopUpButtonStyle}" />

        <sfPopup:SfPopup x:Name="TurbinePopUp">
            <sfPopup:SfPopup.ContentTemplate>
                <DataTemplate>
                    <Grid
                        Padding="10"
                        ColumnDefinitions="*,*"
                        RowDefinitions="*,*,*,*"
                        RowSpacing="20">

                        <Border
                            Grid.ColumnSpan="2"
                            Style="{x:StaticResource CommonBorderStyle}">
                            <Entry
                                Text="{x:Binding Turbine.Name}"
                                TextColor="Black" />
                        </Border>

                        <Border
                            Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Style="{x:StaticResource CommonBorderStyle}">

                            <Grid ColumnDefinitions="*,60">
                                <Entry
                                    IsReadOnly="True"
                                    Text="{x:Binding Turbine.StringifyInstalationDate}"
                                    TextColor="Black" />

                                <Label Grid.Column="1">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding OpenDatePickerCommand, Source={x:RelativeSource AncestorType={x:Type vm:TurbinesCollectionPageViewModel}}}"
                                            CommandParameter="{x:Binding Source={x:Reference picker}}" />
                                    </Label.GestureRecognizers>
                                </Label>
                            </Grid>
                        </Border>

                        <picker:SfDateTimePicker
                            x:Name="picker"
                            Grid.Row="4"
                            Style="{x:DynamicResource DateTimePickerStyle}">
                            <picker:SfDateTimePicker.Behaviors>
                                <mct:EventToCommandBehavior
                                    Command="{x:Binding ConfirmDateCommand,
                                                        Source={x:RelativeSource AncestorType={x:Type vm:TurbinesCollectionPageViewModel}}}"
                                    CommandParameter="{x:Binding SelectedDate,
                                                                 Source={x:Reference picker}}"
                                    EventName="OkButtonClicked" />
                                <mct:EventToCommandBehavior
                                    Command="{Binding CancelDateCommand, Source={x:RelativeSource AncestorType={x:Type vm:TurbinesCollectionPageViewModel}}}"
                                    CommandParameter="{x:Binding Source={x:Reference picker}}"
                                    EventName="CancelButtonClicked" />
                            </picker:SfDateTimePicker.Behaviors>
                        </picker:SfDateTimePicker>

                        <Border
                            Grid.Row="2"
                            Grid.ColumnSpan="2"
                            Style="{x:StaticResource CommonBorderStyle}">
                            <Entry
                                Text="{x:Binding Turbine.Address}"
                                TextColor="Black"
                                VerticalTextAlignment="Center" />
                        </Border>

                        <Button
                            Grid.Row="3"
                            Grid.ColumnSpan="2"
                            Command="{Binding SaveAndCloseCommand, Source={x:RelativeSource AncestorType={x:Type vm:TurbinesCollectionPageViewModel}}}"
                            CommandParameter="{x:Binding Source={x:Reference TurbinePopUp}}"
                            CornerRadius="8"
                            IsEnabled="{x:Binding IsSaveEnable}"
                            VerticalOptions="End" />
                    </Grid>

                </DataTemplate>
            </sfPopup:SfPopup.ContentTemplate>
        </sfPopup:SfPopup>

    </Grid>
</ContentPage>